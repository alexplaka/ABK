% CURVEFITTING
%
% Files

%   FitCurve_Hill      - Fits data to Hill equation y = x^n / (x^n + K)
%                        Output: stdout

%   FitCurve_Hill_g    - Fits data to Hill equation y = A * x^n / (x^n + K)
%                        Output: graph and stdout (MOST RECENT)

%   FitCurve_Hill_gs   - Fits data to Hill equation y = x^n / (x^n + K)
%                        Output: graph
%   FitCurve_Hill_s    - Fits data to Hill equation y = x^n / (x^n + K)
%                        Output: none
%   FitCurve_Hill_fixK - Fits data to Hill equation y = x^n / (x^n + K)
%                        Output: graph
%   FitCurve_Hill_fixn - Fits data to Hill equation y = x^n / (x^n + K)
%                        Output: graph and stdout

%   FitCurve_exp       - Fits data to equation y = A e^(-kx)
%                        Output: graph
%   FitCurve_rec       - Fits data to equation y = 1 / (kx)
%                        Output: graph
%   FitCurve_sqrt      - Fits data to equation y = k x^(1/2)
%                        Output: graph
%   FitCurve_sigmoid   - Fits data to equation y = A (1 - e^(-kx))^h

%   FitCurve_line_g    - Fits data to linear equation y = mx + b
%                        Output: graph and stdout

%   FitCurve_line_s    - Fits data to linear equation y = mx + b
%                        Output: stdout

%   FitCurve_lineb_g   - Fits data to linear equation y = mx + b (b is set)
%                        Output: graph and stdout
%   FitCurve_lineb_s   - Fits data to linear equation y = mx + b (b is set)
%                        Output: none
